type Query {
    personById(id: ID): Person
    # on runtime converted to repository interface method
    personByFirstName(firstName: String): Person
    personByLastName(lastName: String): Person
    persons: [Person]
}

type Person {
    id: ID,
    firstName: String,
    lastName: String
}
